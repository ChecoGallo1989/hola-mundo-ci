name: Alerta CI

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar notificación a ntfy
        run: |
          curl -d "Se ha hecho un push a la rama main del repositorio 🚀" \
          https://ntfy.sh/devops-itla
